import { Fragment } from "react";
import Formback from "./Formback";

const Form = (props) => {
    return (
        <Fragment>
            <Formback />

        </Fragment>
    )
}
export default Form;